{
  "name": "myschedulerapp",
  "version": "1.0.0",
  "description": "A full-stack scheduling application that helps users efficiently plan their time by automatically organizing tasks based on priorities, deadlines, and available time slots.",
  "scripts": {
    "start": "pnpm start:frontend",
    "start:frontend": "cd frontend && ng serve",
    "build": "pnpm build:api-client && pnpm build:frontend",
    "build:frontend": "cd frontend && ng build",
    "build:api-client": "cd api-client && pnpm build",
    "lint": "pnpm lint:frontend",
    "lint:frontend": "cd frontend && pnpm lint:check",
    "format": "pnpm format:frontend",
    "format:frontend": "cd frontend && pnpm format",
    "fix": "pnpm fix:frontend",
    "fix:frontend": "cd frontend && pnpm fix",
    "clean": "pnpm clean:frontend && pnpm clean:api-client && rimraf node_modules",
    "clean:frontend": "cd frontend && rimraf dist node_modules",
    "clean:api-client": "cd api-client && rimraf dist node_modules",
    "dev": "concurrently \"pnpm start:frontend\" \"cd backend && dotnet watch run --no-hot-reload --project src/Api\"",
    "generate:api-client": "cd api-client && pnpm generate"
  },
  "author": "hegedus-mark",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "api-client"
  ],
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1"
  }
}